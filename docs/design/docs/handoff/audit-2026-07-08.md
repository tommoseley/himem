# Spec set audit — July 8 2026

Ran after the Clips-page/ontology work, checking every mock, component, and doc for consistency with the four architecture docs (North Star · Kingfisher Language · shaping model · evidence and context) and the new primary-objects nav. This is the completeness report to pair with `docs/handoff/change-set-2026-07-08.md`.

## Verdict

The spec set is **coherent and complete for handoff**, with the fixes below applied this pass. No contradictions remain between the four architecture docs; the drift was all in older *surface* specs written before the ontology landed, and it's now either fixed or banner-reconciled.

## Fixed this pass

1. **`screens-tutorials.jsx`** — Watch tutorial said "Bundle into a memory" (retired verb). → "Shape it into a memory."
2. **`Captured Clips · session-first · spec.md`** — added a **v4 banner**: this surface is now the `Clips` tab's default view (not a Settings-reached window); the per-card "Make a Memory" pill prose is superseded by the workbench/Sort model + intent vocabulary. The body prose below the banner still describes the old pill model in ~10 places — **intentionally left**, because a full line-by-line rewrite of a heavily-locked doc under one pass is riskier than a clear top-of-doc override. The banner tells CC which prose is dead.

## New mock delivered

- **`HiMem · Clips.html`** (+ `screens-clips-page.jsx`) — the three-tab nav + Clips page (default "New" view with an AI suggestion, loose clips, and the downloading-from-phone state), the "All" view with **"Referenced in,"** and the **clip-as-primary-object** detail (transcript · media · date · Referenced in · Projects). This is the one surface the ontology needed drawn and had no equivalent for.

## Known-open items (flagged, not defects)

These are deliberate gaps or staged work, listed so nothing looks forgotten:

1. **`Captured Clips · session-first · spec.md` body prose** — reconciled by banner, not rewritten. If you want it fully rewritten to the Clips-page model (removing every "Make a Memory pill inside every card" line), that's a dedicated pass — say so. Low urgency: CC has the banner + the change-set doc.
2. **Memories list spec / Flow / older canvases** still describe a **two-tab "Memories ⇄ Projects"** control in places; the decision is now **three-tab Clips · Memories · Projects**. CLAUDE.md · Phone is corrected (source of truth); the surface docs inherit it but weren't line-edited. Worth a sweep when the nav is built.
3. **No standalone "Clips page" spec doc** yet — the rules live across `evidence and context` (ontology), `the shaping model` (flow), CLAUDE.md · Phone (nav/landing), and the new mock. That's sufficient for build; a consolidated `Clips · spec.md` could be written later if CC wants one home. The Captured Clips spec effectively *becomes* that home once its body is rewritten (item 1).
4. **`Himem · Captured Clips (session-first).html`** canvas still renders the old session-list model (with `ScrCCSort`, select-mode). It's current for the *workbench mechanics*; it predates the Clips-tab framing. Not wrong, just narrower than the new `HiMem · Clips.html`. Keep both; the new one is the nav-level view.
5. **Staged, schema-ready, not built** (correct to be absent from mocks): AI-proposed cross-reference/association, cross-appearance "Find the thread," reflection-over-time, Studio.

## Confirmed consistent (no action)

- **Vocabulary** — live mocks are clean of retired verbs ("Make a Memory," "Bundle," "Move to…"); the only remaining hits are in docs that *quote* them as banned, or in `backups/`.
- **Notifications** — Channel B fully retired across CLAUDE.md, Flow, onboarding wizard, and the shaping model.
- **Ontology** — many-to-many clip↔memory reconciled across evidence-and-context, shaping model, Captured Clips Sort-dedup note, and CLAUDE.md.
- **Buttons/color, deletion (bottom full-width), affordance vocabulary, 44px floor** — consistent across `HiMem · Buttons & Actions.html`, the memory-detail specs, and the design system.
- **Kingfisher Language ↔ North Star** — the language layer is named in both, with the noun/verb caution.

## Document map (the stable set)

**Architecture (studio-level, why→what):** `Kingfisher · North Star.md` · `Kingfisher Language.md` · `HiMem · the shaping model.md` · `HiMem · evidence and context.md`
**Project spine:** `CLAUDE.md`
**Surface specs:** `Captured Clips · session-first` · `Memories list` · `Memory Detail · unified editing model` · `Memory Detail · long-memory navigation` · `AI Organize` · `Projects · MVP` · `On a roll` · `Watch` · `Tutorials · triggers` · `Pricing model · Capture-Connect-Create` · `Crucible · topic palette`
**Handoff:** `docs/handoff/change-set-2026-07-08.md` · this audit
