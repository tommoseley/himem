// Replaced by WatchRootView. The Xcode-generated stub is kept as an empty
// file so the project's filesystem-synchronized group reference stays valid;
// the real root view is in WatchRootView.swift.
import SwiftUI

private struct _LegacyContentViewPlaceholder: View {
    var body: some View { EmptyView() }
}
